name: Frontend CI/CD
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# --- DETTA ÄR VAD DU SAKNAR ---
permissions:
  packages: write
  contents: read
# ------------------------------

jobs:
  test-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Build and push Docker image
        if: github.ref == 'refs/heads/main'
        run: |
          # Jag ser att du har " [file:6]" i slutet av din kod i frågan.
          # Se till att det INTE är med i din riktiga fil!
          docker build -t ghcr.io/${{ github.repository }}:${{ github.sha }} .
          docker tag ghcr.io/${{ github.repository }}:${{ github.sha }} ghcr.io/${{ github.repository }}:latest
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          docker push ghcr.io/${{ github.repository }}:${{ github.sha }}
          docker push ghcr.io/${{ github.repository }}:latest

  deploy:
    needs: test-build
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to CBH Cloud
        run: |
          # Lägg till -k för att tillåta self-signed certifikat
          curl -k -X POST https://deploy.cloud.cbh.kth.se/api/v1/deployments/frontend/triggers \
            -H "Authorization: Bearer ${{ secrets.KTH_CLOUD_API_KEY }}"
  
